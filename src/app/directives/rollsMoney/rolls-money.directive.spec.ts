import { RollsMoneyDirective } from './rolls-money.directive';

describe('RollsMoneyDirective', () => {
  it('should create an instance', () => {
    const directive = new RollsMoneyDirective();
    expect(directive).toBeTruthy();
  });
});
